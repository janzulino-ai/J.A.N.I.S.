<Window x:Class="JANIS.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="J.A.N.I.S." Height="720" Width="1180" MinHeight="520" MinWidth="900"
        Background="{StaticResource BgDark}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="200"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0" Background="{StaticResource Panel}" BorderBrush="{StaticResource BorderLine}" BorderThickness="0,0,1,0">
      <DockPanel>
        <StackPanel DockPanel.Dock="Top" Margin="16,20,16,12">
          <TextBlock Text="J.A.N.I.S." FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentCyan}"/>
          <TextBlock Text="Just Another Neuralgic Improving Server" FontSize="9" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,4,0,8"/>
          <TextBlock x:Name="StatusPill" Text="…" FontWeight="Bold" FontSize="11"/>
        </StackPanel>
        <ListBox x:Name="NavList" SelectionChanged="NavList_SelectionChanged"
                 Background="Transparent" BorderThickness="0" Margin="8,0">
          <ListBoxItem Tag="agent" Content="⚡ Agent"/>
          <ListBoxItem Tag="setup" Content="⚙ Requisiti"/>
          <ListBoxItem Tag="chat" Content="💬 Chat"/>
          <ListBoxItem Tag="preview" Content="◈ HUD / Anteprima"/>
          <ListBoxItem Tag="terminal" Content="▸ Terminale"/>
          <ListBoxItem Tag="browser" Content="🌐 Browser"/>
          <ListBoxItem Tag="plugins" Content="🔌 Plugin / Tool"/>
          <ListBoxItem Tag="settings" Content="⚡ Impostazioni"/>
        </ListBox>
      </DockPanel>
    </Border>

    <ContentControl x:Name="ContentHost" Grid.Column="1"/>
  </Grid>
</Window>
